{ Sample program
  in TINY language -
  computes factorial
}
read x; { input an integer }
  /* this is the test
  multlines comment */
read y;
if 0 < x then { don't compute if x <= 0 }
  {fact := 1.89;}
  repeat
    fact := fact * x; 
    {y := "string";}
    x := x - 1
  until x = 0;
  write fact  { output factorial of x }
end
